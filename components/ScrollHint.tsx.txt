"use client";

export default function ScrollHint() {
  return (
    <div className="glass fixed bottom-4 left-1/2 z-40 -translate-x-1/2 rounded-full px-4 py-2 text-xs font-medium text-[var(--muted)]">
      swipe / scroll ↓
    </div>
  );
}
